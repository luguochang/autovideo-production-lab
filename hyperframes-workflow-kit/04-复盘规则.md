# 复盘规则

完整视频通过用户验收后，在项目内写 `RETROSPECTIVE.md`：

- 记录成功的基础风格、组件家族、官方 registry item 和 motion rule。
- 记录失败模式、原因、具体修正和用户原话反馈。
- 区分项目特例与可复用规则；同类问题重复出现后再升级为共享规则。
- 只有经过真实视频验证的规则才可回写 `style-library/`。
- 未验证规范不得提升为全局 Codex skill。

