# RAG 全链路语义动效重构任务

## 收口目标

- 保留已人工听审通过的主体旁白，不覆盖旧音频、旧成片或旧标准包。
- 在 `794.88s` 的离线/在线语义分界拆成上下两集。
- 上集补一段收束旁白，下集补一段回顾旁白；两段使用原项目 CosyVoice 预设 14，仅作为内部听审候选，等待最终人工听审。
- 把“左人物 + 右假终端/三栏框/大字卡”的重复页面，改为按 RAG 阶段组织的稳定语义画布。
- 先完成五段代表性样片、自测和人工可视预览，再扩展到上下集。
- 不新增批量项目、P1/P2 功能、视频框架或额外审计系统。

## 视觉合同

- 输出固定为 `1920x1080 / 30fps / 16:9`。
- 基础风格继续使用 `modern-ip-host-explainer / light-apricot`，保留原人物和配色资产。
- 本项目获得用户明确覆盖：人物只在章节开场、过渡和总结出现；语义图需要空间时使用全宽主画布。
- 顶部固定显示当前阶段；底部仅显示精确字幕；主画面不复制完整口播。
- 一次只表达一个核心关系，主画面最多出现 2-5 个短标签。
- 禁止假终端、假代码、假 `INPUT / PROCESS / OUTPUT` 界面和无语义卡片网格。
- 只有真实文档、证据片段或确有边界含义的对象才使用容器；关键词默认无框。
- 章节内部尽量保留同一张总图，通过节点建立、路径推进、聚焦和替换解释内容。
- 新旧完整面板不得重叠；章节之间使用硬切或遮罩覆盖，不使用整页交叉淡化。

## 分集合同

- 上集主体：原正式音频 `0.00s` 到 `794.88s`，覆盖离线建库。
- 下集主体：原正式音频 `794.88s` 到 `1880.218458s`，覆盖在线链路与总结。
- 上集章节：总览、文档解析、分块、上下文增强、向量化、向量库与索引、上集收束。
- 下集章节：上集回顾、查询理解、混合检索、RRF 融合、重排、智能体决策、生成与引用、评测闭环、上线工程、全链总结。

## 自测门禁

- 五段样片必须覆盖：文档解析、分块、混合检索、智能体闭环、评测。
- 每段样片使用同一正式旁白中的真实窗口，不使用占位音频。
- `hyperframes check --snapshots` 无阻断问题。
- 抽帧人工检查：无重叠、无单字框、无整段口播上屏、无假终端、画面焦点明确。
- 样片门通过后才生成上下集完整内部审片版本。
- 最终只交付内部审片；公开发布继续阻断。

## 基线绑定

- 主体旁白 SHA-256：`8bc44127b6d4e925491a4d9bc8e09f7abb223b237075c633c48e8d167ea17702`
- 主体对齐 SHA-256：`e3193c3da74e3ef6d0ae0672ca352513cc8268ce1aafff5658574d84198fe572`
- 旧内部成片继续保留，不作为新视觉验收基线。

