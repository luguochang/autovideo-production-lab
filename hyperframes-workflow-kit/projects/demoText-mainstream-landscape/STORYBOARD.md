# 主流横屏六风格 20 秒执行分镜

| Time | Exact narration / summary | Screen text type | Shared visual intent | Motion source |
|---:|---|---|---|---|
| 0.0-3.7 | 第一，平台把系统的复杂性给隐藏起来。 | `exact-source` | Host and hook appear on one continuous illustrated world | `viewport-change`, `css-marker-patterns` |
| 3.7-8.3 | 低代码、无代码、AI平台、工作流工具， | `exact-source` | Keywords attach to the host and begin a visible chain | `svg-path-draw`, `card-morph-anchor` |
| 8.3-14.5 | 把模型调用、API连接、向量库、工具链调度流程 | `exact-source` | Hidden layers pull out from the easy surface | `svg-path-draw`, `viewport-change` |
| 14.5-20.0 | 会拖节点，不等于懂系统。 | `generated-summary` | Short visual punch resolves the accumulated relationship | `card-morph-anchor` |

All six candidates keep the host, base world, first-layer labels, and bottom caption rail alive through the terminal frame. They do not clear into unrelated title cards.
