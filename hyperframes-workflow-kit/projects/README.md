# Projects

每个视频使用一个独立子目录。使用仓库根命令 `npm.cmd run video:new -- ...` 创建，不要手工复制一个旧项目后改名。

项目至少包含 `NarrationLock.json`、`project-state.json`、`VIDEO_TASK.md`、`STYLE_ANALYSIS.md`、`STYLE_REVIEW.md`、`STORYBOARD.md`、`AssetManifest.json` 和 HyperFrames composition；渲染缓存与 `node_modules` 不进入分享包。
