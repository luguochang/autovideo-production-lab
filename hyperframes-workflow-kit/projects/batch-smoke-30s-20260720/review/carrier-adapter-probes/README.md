# Primary Carrier Adapter Technical Probes

These three 6.35-second projects are internal component regressions only:

- `data-proof/`
- `code-surface/`
- `device-surface/`

They verify that bounded local adapters render inside `content.right` while the approved Candidate A shell remains fixed: supplied Q-version host in `host.left`, `#F2DFC7` stage, and persistent captions. Their payloads are `illustrative-mock`; they do not prove a factual claim, production media right, motion-recipe lifecycle approval, human review, or public-release readiness.

Each directory contains `technical-check.strict.json`, produced with HyperFrames `0.7.64` using strict runtime, layout, motion, contrast, dense sampling, and transition sampling. A successful component check never promotes these probes into `experiments/E*-*probe/`, which remains the formal motion lifecycle path.

Machine checks current on 2026-07-21:

- `data-proof`: `ok=true`, zero lint/runtime/layout/motion/contrast findings.
- `code-surface`: `ok=true`, zero lint/runtime/layout/motion/contrast findings.
- `device-surface`: `ok=true`, zero lint/runtime/layout/motion/contrast findings.

Human review stays pending. Do not copy a mock evidence label into a formal shot as `verified`; formal carriers require a project entity receipt `{kind, id, sha256}` that the compiler can resolve against the claim, source, or media ledger.
