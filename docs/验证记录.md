# 验证记录

## 2026-07-15 初始视觉原型

- 完成 1080x1920、30fps Remotion 模板。
- TypeScript 严格检查通过。
- 成功渲染 810 帧、约 27 秒 H.264 MP4，文件约 2.1 MB。
- Chrome Headless Shell 已自动安装。
- 本地 Studio 运行在 `http://localhost:3100`。
- GitHub `heygen-com/hyperframes` 远端 HEAD 可访问；上游仓库不属于当前运行时依赖。

## 2026-07-15 真实资料端到端验证

- 输入《EchoFlow Voice Agent 面试学习资料》，兼容接口成功生成 7 个结构化场景。
- Edge TTS 使用 `zh-CN-YunxiNeural`，成功生成逐场景 WebM 音频和词级时间戳。
- TTS 脚本加入三次重试，处理 Edge 服务临时 WebSocket 中断。
- 根据实际音频时间戳生成 3268 帧时间线并完成渲染。
- 最终文件：1080x1920、30fps、108.93 秒、10,134,316 字节。
- 视频轨：H.264；音频轨：AAC、48kHz、双声道。
- 字幕按词级时间戳滚动，并高亮当前词。
- TypeScript 检查和 1080x1920 抽帧检查通过。

## 已知问题与下一步

- 当前 109 秒超过 45-75 秒目标，需要加入总字数和时长预算校验，并在超限时自动压缩口播。
- 当前模板已有 hook、流程、列表和总结，后续应增加架构图、代码片段和对比图模板。
- WebM 音频在 Remotion 中有随机访问性能提示，不影响最终 MP4；批量生产可转为 WAV 以提升预览定位性能。
- 品牌、BGM、抖音发布和授权音色仍待确定。

## 2026-07-15 单画布切换修复

- 第二条 RAG 参考视频为 1152x720、137.09 秒，确认其背景、栏目标题和字幕层始终常驻。
- 移除场景整体淡出，改为单一常驻背景，只对局部文本、标签、流程节点和插图执行动画。
- 新增已审稿口播模式：口播由确定性代码切分，模型只能生成视觉元数据，不能改写原文。
- 重新渲染 `reference-style.mp4`，FFmpeg 全片黑帧检测结果为 `BLACK_FRAMES=NONE`。

## 2026-07-15 连续画板参考分析

- 第三个参考视频为 1280x720、230.78 秒。
- 连续帧确认其采用常驻纸张背景、人物锚点和字幕层，只对独立对象执行新增、移动、缩放、强调和移除。
- 当前分页 beat 模型即使没有黑闪，仍无法自然表达对象跨段持续存在。
- 建议新建 Motion Canvas 白板引擎，或在 Remotion 中重写对象注册表、世界坐标与相机关键帧系统。

## 2026-07-15 连续画板技术样片

- 新增 `CanvasStyleDemo`，使用 2200x1050 世界画板和连续相机关键帧。
- 旧对象会缩小、移动并跨讲解阶段继续存在，新对象在同一画板空白区域展开。
- 输出 31.07 秒、1280x720、H.264 + AAC 样片。
- FFmpeg 全片黑帧检测结果为 `BLACK_FRAMES=NONE`。
