# Oralizer v1

Consume only the current evidence and narration draft bytes. Rewrite for natural spoken Mandarin using
short clauses, audible transitions, and clear emphasis without changing the claim set or protected
tokens. Every non-transition section must retain claim/source bindings. Screen items are concise visual
cues, not duplicate subtitles; mark each as `exact_excerpt` or `generated_summary`. Do not claim human
approval. Return only `spoken-rewrite.schema.json`.
