# E20 Probe Review

Status: `candidate` / `technical-evidence-ready` / `human review pending`

The internal MP4, deterministic snapshots, strict check, decode QA, and imported-file audio QA are
complete. No audio playback was performed and no human approval was produced.

## Intent

The narration explains that a simple interface hides a longer system. The screen uses one persistent layers object rather than a card wall: it begins as the visible entry, follows a drawn route through model, API, data, and orchestration, then lands on a held conclusion.

Exact narration stays in the caption rail. The title, station labels, details, and conclusion are generated summaries; they do not replace NarrationLock.

## Screen copy receipt

- Headline: `一个可见入口，藏着四层系统`
- Entry: `入口 / 用户只看见按钮和节点`
- Model: `模型 / 接住意图 / 生成能力`
- API: `API / 连接服务 / 交换上下文`
- Data: `数据 / 检索知识 / 保存状态`
- Orchestration: `编排 / 调度工具 / 完成动作`
- Terminal conclusion: `入口只是表层，系统靠四层协作`

## Evidence and decisions still missing

- explicit human visual decision
- lifecycle `probe-passed` acceptance
- project approval for a real production project

## Human checklist

- [ ] The Q-version host remains stable in `host.left` and is never moved by the content camera.
- [ ] The layers icon is visibly one persistent object, not repeated station artwork.
- [ ] The object and camera read as one continuous journey rather than slide cuts.
- [ ] Route strokes draw continuously before or during each transfer.
- [ ] Model, API, data, and orchestration are legible without becoming a card wall.
- [ ] The caption rail stays fixed, exact, and visually subordinate.
- [ ] The terminal frame remains static and readable for the full final second.
- [ ] No audio test uses a microphone, recording, autoplay, playback call, or external sound output.

## Decision

No decision has been made. This file intentionally does not mark the recipe approved; strict/render QA and a real human review must happen before lifecycle apply or promotion.
